/**
 * @file
 * Javascripts.
 */

(function (Drupal, kalathings, $, window) {
  'use strict';
  Drupal.behaviors.kalathings = {
    attach: function (context, settings) {
      // Code me please.
    }
  };

}(Drupal, window.kalathings = window.kalathings || {}, window.jQuery, window));
